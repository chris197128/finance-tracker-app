# Finance Tracker
This finance tracker allows the user to keep track of their expenses and income. It utilizes JavaScript, HTML, and CSS for the frontend, JavaScript for the backend (using SQL to handle transaction data), and Express JS to handle routing from the frontend to backend.
